# 2026-03-27 工作日志

## 12:45 — 强制拉取远程仓库最新版本
- 执行 `git fetch origin && git reset --hard origin/main`
- HEAD 重置到 `599b02b`（提交信息：优化拉取逻辑，更新版本日志）
- 本地修改（NETWORK_OPTIMIZATION.md、src/components/NetworkStatus.jsx）被远程版本覆盖

## 12:50 — 修复 base-schedules.json 拉取数据陈旧问题
- 根因：主URL使用 jsDelivr CDN (`@main` 分支)，CDN 刷新周期约 24 小时，导致数据仓库更新后无法及时拿到新数据
- 修复：`src/constants.js` 主URL改为 `gh-proxy.org` 代理 `raw.githubusercontent.com`（实时数据），jsDelivr 系列降为后备
- 同步更新 `NETWORK_OPTIMIZATION.md` 说明文档

## 13:08 — 提交并推送到 GitHub
- 修正 `changelog-data.js` 中 v1.2.5 描述（去掉多余的"依旧"）
- 提交信息："优化拉取逻辑，避免中转站的缓存导致日程更新延迟"
- 成功推送至 `origin/main`（56255c3）

## 13:13 — 在日历视图添加更新基础日程库按钮
- 提取设置中的更新逻辑为 `handleUpdateBaseSchedules` 函数
- 在日历视图的按钮区域（添加日历日程和导出本地备份之间）添加更新按钮
- 按钮样式：indigo-600 背景，带刷新图标和加载动画

## 13:18 — 合并数据同步按钮为下拉菜单
- 将"导出本地备份"按钮改为"数据同步"下拉菜单
- 菜单包含：导出本地备份、上传到 Gist、上传到云端、更多同步设置...
- 添加点击外部自动关闭菜单的功能
- 如果未配置 Token 或 SyncID，点击上传选项会提示并跳转到设置页面
