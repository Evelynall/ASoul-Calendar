# 2026-05-11

## ScheduleCard.jsx 按钮改为 <a> 标签

将"跳转链接"和"观看官方录播"两个按钮从 `<button>` 改为 `<a target="_blank" rel="noopener noreferrer">` 标签，以改善 B 站视频链接跳转问题（"视频已失效"）。已提交并推送至 GitHub（commit: eb1a959）。

> 注：此前尝试过多种方案均无效（去掉 noreferrer、window.open、share_source 参数、http 替换等），B 站视频失效问题根本原因尚未确认。

## 录播按钮优先级逻辑 + 新图标

- **ScheduleCard.jsx**：直播间按钮加了 `!hasOfficialRecord` 条件，当日程存在 `officialRecordUrl` 时不显示直播间按钮
- **Icon.jsx**：新增 `record-play` 图标（胶卷主体+左右齿孔+中心播放三角），录播按钮由 `bilibili` 图标改用此图标
